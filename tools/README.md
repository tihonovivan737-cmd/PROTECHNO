# Tools

## cloudflared

`cloudflared.exe` intentionally is not stored in Git to keep the repository lightweight.

Download latest Windows binary from:
https://github.com/cloudflare/cloudflared/releases

Place `cloudflared.exe` into `tools/` if you want to run tunnel commands from `HOSTING.md`.
